import 'package:flutter/material.dart';

class RewardsFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Rewards Fragment Screen"),
      ),
    );
  }
}
