import 'package:flutter/material.dart';

class ScanQRFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Scan QR Fragment Screen"),
      ),
    );
  }
}
